<html lang="en">
  <head>
    {{>header}}
    <script src="./js/login.js"></script>
    <style>
      body{
        background-color:#495159;
      }
    </style>

    <title>Admin Register</title>
  </head>
  <body>
    <!--Main-->
    

    <div class="wrapper fadeInDown" style="background-color: rgb(21, 34, 107)">
      <div id="formContent">
        <!-- Tabs Titles -->

        <!-- Icon -->
        {{#if messages.error}}
          <span class="registerSpan">{{messages.error}}</span><br>
        {{/if}}

        
        <h2>Register</h2>

        <!-- Login Form -->
        <form action="/api/users/register" method="POST">
          <input
            type="name"
            id="email"
            class="fadeIn second"
            name="name"
            placeholder="Name"
            value="{{messages.name}}"
          />
          <input
            type="email"
            id="email"
            class="fadeIn second"
            name="email"
            placeholder="Email"
            value="{{messages.email}}"
          />
          <input
            type="tel"
            id="email"
            class="fadeIn second"
            name="ph_number"
            placeholder="Ph Number"
            value="{{messages.ph_number}}"
          />
          <input
            type="password"
            id="password"
            class="fadeIn third"
            name="password"
            placeholder="Password"
          />
          <input
            type="password"
            id="password"
            class="fadeIn third"
            name="confirm_password"
            placeholder="Confirm Password"
          />
          <button type="submit" class="fadeIn fourth myBtn" value="Register">Register</button>
        </form>

        <!-- Remind Passowrd -->
        <div id="formFooter">
          
          <a class="underlineHover" href="/">Return to hompage</a>
        </div>
      </div>
    </div>
  </body>
</html>